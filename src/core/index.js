// src/core/ — pure JS only. No DOM, no jQuery. Will host BinaryReader/Writer,
// RLE codec, ThingType, ThingCategory, Version, animation primitives. Stage 2+.

export const LAYER_NAME = "core";

console.log(`[ObjectBuilder-JS] layer loaded: ${LAYER_NAME}`);
